# Dockerfile.cross-windows — cross 交叉编译 Windows (GNU) 目标的自定义镜像
# cross 官方 x86_64-pc-windows-gnu 镜像只提供 amd64 manifest；
# Apple Silicon 主机上 Docker 默认请求 arm64 会拉取失败，
# 因此强制 --platform=linux/amd64（经 Rosetta/qemu 模拟运行）。
FROM --platform=linux/amd64 ghcr.io/cross-rs/x86_64-pc-windows-gnu:main
